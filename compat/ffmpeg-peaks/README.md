# ffmpeg-peaks

This package preserves the former name of [Peakwright](https://www.npmjs.com/package/peakwright).

Existing imports and the `ffmpeg-peaks` command continue to work. New projects should install `peakwright` and use the `peakwright` command.
