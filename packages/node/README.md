# @peakwright/node

Peakwright for Node.js and Bun. It decodes WAV natively with zero third-party runtime dependencies and uses an installed FFmpeg binary only when another format or conversion requires it.

```sh
bun add @peakwright/node
```
