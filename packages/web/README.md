# @peakwright/web

Peakwright for browsers and web runtimes. It generates waveform data from WAV `Blob` and `ArrayBuffer` inputs with zero third-party runtime dependencies.

```sh
bun add @peakwright/web
```
